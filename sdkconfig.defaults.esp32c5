# ESP32-C5. Dual-band, and the full -24 dBm ladder like the C3.
#
# Preview: v1.0 silicon wants ESP-IDF v5.5.2 or newer. It builds on v5.5.
# Console on the native USB Serial/JTAG peripheral, so UART1 (the FC link) and
# UART0 both stay free.
CONFIG_ESP_CONSOLE_USB_SERIAL_JTAG=y
